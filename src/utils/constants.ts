export enum generalConstants {
  shopTitle = 'Welcome to the shop',
  emptyCart = 'Your cart is empty! please add something to the cart',
  totalLabel = 'Total',
}

export enum cartConstants {
  addToCart = 'add to cart',
  goToChart = 'go to cart',
  backtoShop = 'back to shop',
}

export enum actionTypes {
  addToCartACtion = 'addToCart',
  deleteFromCartAction = 'deleteFromCart',
  fetchPhotoAction = 'fetchPhotos',
}

export enum tableHeaderConstants {
  productName = 'Product name',
  productPrice = 'Price',
  productQuantity = 'Quantity',
  actions = '',
}
